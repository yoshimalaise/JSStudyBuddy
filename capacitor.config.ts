import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codeSchoolInABox.JSStudyBuddy',
  appName: 'JSStudyBuddy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
